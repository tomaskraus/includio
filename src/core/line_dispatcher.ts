export const createLineDispatcher = () => {
  return (cmdLine: string, input: string[]) => {
    // const [contentSelectorStr, ...commandLines] = tagContent.split('|');
    // const sourceLines = await getLines(contentSelectorStr);
    return input;
  };
};
